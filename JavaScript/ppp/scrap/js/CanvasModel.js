function CanvasModel () {
    this.layers = [];
    
    var activeLayer = 0;
    
    // set pixel data in the active layer
    this.setPixel = function(x, y, paletteIndex, alpha) {
    
    };
    
    // return pixel data in form of {index:#, alpha: #.##}
    this.getPixel = function(x,y) {
    
    };
    
    // adds layer above selected layer
    this.addLayer = function() {
    
    };
    
    // removes active layer unless last layer
    this.removeLayer = function() {
    
    };

}